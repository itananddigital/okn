✅ Solution
1. Fix permissions for entire Superset directory

Run this from the parent folder:

sudo chown -R $USER:$USER Superset-Staging/


Or explicitly:

sudo chown -R ig:ig /home/ig/Superset-Staging/

2. Also fix permissions inside Superset frontend

Webpack is failing here:

superset/static/assets
superset-frontend/.temp_cache/


Run:

sudo chown -R ig:ig /home/ig/Superset-Staging/superset/static/assets
sudo chown -R ig:ig /home/ig/Superset-Staging/superset-frontend/.temp_cache

3. Clean and rebuild

Inside superset-frontend:

rm -rf ~/.cache/yarn
rm -rf node_modules
rm -rf superset/static/assets/*
rm -rf .temp_cache


Now install again:

yarn install


Then build:

npm run build

⚙️ Extra: If you used Docker before

If those files were generated by Docker or root, fix with:

sudo find . -type f -exec chmod 664 {} \;
sudo find . -type d -exec chmod 775 {} \;
